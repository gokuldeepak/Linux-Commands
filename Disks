#Format to ext4
 sudo mkfs.ext4 /dev/sdb
 
#Mount Disk 
sudo mount /dev/sdb /mnt/anydiskname

#Mount automatically after every reboot
nano /etc/fstab
/dev/sdb     /mnt/sdb      ext4        defaults      0       0


#Unmount the disk
 sudo umount /dev/sdb
